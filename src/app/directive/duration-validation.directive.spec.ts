import { DurationValidationDirective } from './duration-validation.directive';

describe('DurationValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DurationValidationDirective();
    expect(directive).toBeTruthy();
  });
});
